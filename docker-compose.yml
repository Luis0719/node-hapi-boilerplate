services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: foxadmin
      POSTGRES_PASSWORD: asquell.123
      POSTGRES_DB: fox_dev
    volumnes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - '5432:5432'
  web:
    image:
